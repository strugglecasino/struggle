import React from 'react';
const BetboxTarget = ({bet, onChange, multiplierToWinProb}) => {

    return (
        <section className="betbox_targets">

        </section>
    );
}

export default BetboxTarget;