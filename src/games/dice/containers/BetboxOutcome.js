import React from 'react';
import Slider from '../components/Slider';

const BetboxOutcome = ({bets, onStoreChange}) => {

    return (
        <section className="betbox_outcome">

            <span id="tooltip"> 00.00 </span>
        </section>
    );
}

export default BetboxOutcome;